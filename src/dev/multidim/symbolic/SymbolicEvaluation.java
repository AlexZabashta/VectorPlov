package dev.multidim.symbolic;

import dev.multidim.Evaluation;
import dev.multidim.size.Dim;

public class SymbolicEvaluation implements Evaluation<SymNode> {

    @Override
    public SymNode sum(Dim dim, SymNode... variables) {
        return null;
    }

}
